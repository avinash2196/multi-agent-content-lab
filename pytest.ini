[pytest]
# Automatically apply asyncio mode to all async test functions.
# Required by pytest-asyncio >= 0.21; avoids the need for @pytest.mark.asyncio
# on every individual async test.
asyncio_mode = auto

# Where to find tests
testpaths = tests

# Shorter tracebacks in output by default
addopts = --tb=short
